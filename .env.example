# .env.example
# 请将此文件复制为.env，并填写实际的密钥和配置,先填写POLYMARKET_PRIVATE_KEY然后运行api_key.py生成其他密钥
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=
POLYMARKET_PRIVATE_KEY=
POLYMARKET_SIGNATURE_TYPE=2
POLYMARKET_FUNDER=
# 其他环境变量示例
# Bot Configuration
# Maximum combined cost to trigger arbitrage (e.g., 0.991 means buy if UP + DOWN < $0.991)
TARGET_PAIR_COST=0.99

# Keep this much USDC as a safety buffer when checking whether a trade can be funded
BALANCE_SLACK=0.15

# Refresh cached balance every N seconds; opportunity execution reads only the cache
BALANCE_REFRESH_SECONDS=5

# Number of shares to buy per trade
ORDER_SIZE=5

# Simulation mode (true = no real orders, false = live trading)
DRY_RUN=false

# Optional: Market slug (leave empty for auto-discovery)
POLYMARKET_MARKET_SLUG=

# Maximum number of arbitrage trades per market (1-10, set to 0 for unlimited)
# Example: if set to 1, only execute one trade per 15-minute market
MAX_TRADES_PER_MARKET=1

# Minimum time remaining (in minutes) to execute trades
# If market has less than this time remaining, skip trading
# Example: if set to 1, don't trade when less than 1 minute remaining
MIN_TIME_REMAINING_MINUTES=5